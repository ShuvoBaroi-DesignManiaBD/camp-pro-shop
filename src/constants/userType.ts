export const USER_ROLE = {
    customer: 'customer',
    admin: 'admin',
} as const;

export const user_role = {
    customer: 'customer',
    admin: 'admin',
}

export type TRoles = 'customer' | 'admin'